th:object="${****}"
th:value="*{属性名称}" th:field="*{属性名称}" 
th:value="${user.属性名称}" th:field="${user.属性名称}" 
可行__且有数据回显 [可以用来做修改使用] 
th:value="${属性名称}" 页面可加载,不过没有数据回显 [可以修改,不回显数据] 
th:field="${属性名称}" 页面直接无法加载

简单表达式 （simple expressions）
　　${...}  变量表达式
　　*{...}  选择变量表达式
　　#{...}  消息表达式
　　@{...}  链接url表达式